{
  "dependencies": {
    "@kos-kit/rdf-resource": "2.0.8",
    "@kos-kit/rdf-utils": "2.0.8",
    "@rdfjs/types": "^1.1.0",
    "@types/n3": "^1.16.4",
    "n3": "^1.17.3"
  },
  "engines": {
    "node": ">=20"
  },
  "main": "index.js",
  "files": [
    "*.d.ts",
    "*.d.ts.map",
    "*.js",
    "*.js.map",
    "LICENSE",
    "package.json"
  ],
  "license": "GPL-3.0-only",
  "name": "@kos-kit/mem-models",
  "scripts": {
    "build": "tsc -b && cp LICENSE package.json dist/",
    "clean": "rimraf dist",
    "link": "cd dist && npm link",
    "publish:dist": "cd dist && npm publish --access public",
    "rebuild": "rimraf dist && tsc -b && cp LICENSE package.json dist/",
    "unlink": "npm unlink -g @kos-kit/mem-models",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "watch": "mkdir -p dist && cp LICENSE package.json dist/ && tsc -w --preserveWatchOutput"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kos-kit/lib"
  },
  "type": "module",
  "types": "index.d.ts",
  "version": "2.0.8"
}
